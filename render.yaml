services:
  - type: web
    name: smart-community-health-monitoring
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3000
      - key: SESSION_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
